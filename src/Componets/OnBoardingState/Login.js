import React from 'react';

function Login (props){
    return (
        <div>
            <h1>hello continue with login</h1>
            <button onClick={props.onClick}>Login</button>
        </div>
    )
}
export default Login;